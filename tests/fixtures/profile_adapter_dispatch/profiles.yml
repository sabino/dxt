profile_adapter_dispatch:
  target: pg
  outputs:
    pg:
      type: postgres
      schema: analytics
    duck:
      type: duckdb
      path: local.duckdb
      schema: main
    rs:
      type: redshift
      schema: analytics
