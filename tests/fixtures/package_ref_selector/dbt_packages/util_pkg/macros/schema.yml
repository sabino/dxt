version: 2

macros:
  - name: format_id
    description: Format an identifier in the utility package.
    arguments:
      - name: column_name
        type: string
        description: Identifier expression to cast.
