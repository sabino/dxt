version: 2

sources:
  - name: raw
    tables:
      - name: customers
        columns:
          - name: customer_id
      - name: orders

models:
  - name: stg_customers
    description: Staged raw customers
    config:
      tags: ["staging"]
